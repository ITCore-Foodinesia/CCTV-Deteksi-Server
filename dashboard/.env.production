# Production config - used when built for deployment or via Cloudflare Tunnel
# API tunnel handles routing to Unified Server on port 5001
VITE_API_URL=https://api.foodiserver.my.id
VITE_EDGE_URL=https://api.foodiserver.my.id